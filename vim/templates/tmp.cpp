#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
typedef pair<int, int> pii;

const int MOD = 1000000007;

int mod(int x, int m) { if (x < 0) x += m; return x % m; }

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    return 0;
}

